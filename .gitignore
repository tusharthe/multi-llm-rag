# Secrets — never commit the API key
.env

# Persisted vector store (rebuilt from documents)
store/
chroma_db/

# Local source documents for reindex --rebuild (not committed)
docs/

# Raw chat records (per-chat JSON with user questions + model answers)
chats/

# Log files — contain document text, prompts and model responses
logs/

# Python
__pycache__/
*.pyc
venv/
.venv/

# OS / editor
.DS_Store
.vscode/
.idea/
