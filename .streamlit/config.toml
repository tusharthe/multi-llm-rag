[theme]
# Tokens mirror the Stitch design export (rag_hub_core/DESIGN.md).
base = "dark"

primaryColor = "#d92d34"
backgroundColor = "#262326"
secondaryBackgroundColor = "#2e2a2c"
textColor = "#ece7e4"
linkColor = "#6fc3d8"
borderColor = "#463f43"
codeBackgroundColor = "#2e2a2c"

# Shape language: 12px base radius on cards/inputs/buttons.
baseRadius = "12px"
buttonRadius = "12px"
showWidgetBorder = true
showSidebarBorder = true

# Inter for body/headings, Geist Mono for technical metadata.
font = "Inter:https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap"
headingFont = "Inter:https://fonts.googleapis.com/css2?family=Inter:wght@600;700;800&display=swap"
codeFont = "'Geist Mono':https://fonts.googleapis.com/css2?family=Geist+Mono:wght@400;500;600&display=swap"
baseFontSize = 15
codeFontSize = "13px"
headingFontSizes = ["32px", "24px", "18px", "16px", "14px", "12px"]
headingFontWeights = [700, 600, 600, 600, 500, 500]
linkUnderline = false

# Status colors used by st.badge and :green-badge[...] markdown.
greenColor = "#4caf7d"
orangeColor = "#e09a4a"
redColor = "#d92d34"
blueColor = "#6fc3d8"
grayColor = "#a8a29e"

# Per-model series colors for the analytics charts.
chartCategoricalColors = ["#10a37f", "#d97757", "#4285f4", "#b7131a", "#006578"]

[theme.sidebar]
backgroundColor = "#1b1819"
secondaryBackgroundColor = "#262223"
borderColor = "#3a3335"


[browser]
gatherUsageStats = false


[client]
# "auto" hides Settings when the app is opened via a non-localhost URL
# (LAN IP / hostname resolve to the viewer toolbar). Pin "developer" so the
# ⋮ menu always shows Settings → Appearance (the theme switch).
toolbarMode = "developer"


[server]
# Automatically re-runs the script when a file is modified on disk
runOnSave = true

# Options: "auto", "watchdog", "poll", or "none"
fileWatcherType = "auto"
